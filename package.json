{
  "name": "@ngx-rapid/parent",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "version:bump:prerelease": "lerna publish --cd-version=prerelease --skip-git --skip-npm --yes",
    "clean": "lerna clean --yes && lerna run clean",
    "install": "lerna bootstrap",
    "build": "lerna run build",
    "docs": "lerna run docs",
    "site": "lerna run site",
    "package": "npm run build && npm run docs && npm run site",
    "publish": "lerna publish --repo-version"
  },
  "author": "Piotr Kosmowski",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^2.7.1"
  }
}
