{
  "name": "ngx-rapid",
  "private": true,
  "description": "",
  "scripts": {
    "clean": "lerna clean --yes && lerna run clean",
    "install": "lerna bootstrap",
    "build": "lerna run build",
    "site": "lerna run --scope @ngx-rapid/demo build && lerna run docs && lerna run site",
    "deploy": "lerna run --scope @ngx-rapid/site deploy",
    "package": "npm run build && npm run site",
    "publish:prerelease": "lerna publish --cd-version=prerelease --force-publish=* --yes && npm run site && npm run deploy"
  },
  "author": "Piotr Kosmowski",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^2.7.1"
  }
}
